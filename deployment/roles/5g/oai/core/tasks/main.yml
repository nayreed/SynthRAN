---
- name: Materialize the reviewed OAI core lifecycle reference
  ansible.builtin.include_tasks: materialize_reference.yml

- name: Delegate the reference-equivalent OAI core lifecycle
  ansible.builtin.include_tasks: "{{ synthran_oai_core_reference_tasks }}"
